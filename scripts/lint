#!/bin/bash
set -e

# ─── Lint ────────────────────────────────────────────────────────────────────

run_lint() {
  echo "🔍 Linting project..."
  uvx ruff check "$@"
  echo "✅ Lint complete."
}

if [[ "${#BASH_SOURCE[@]}" -eq 1 ]]; then
  run_lint "$@"
fi
