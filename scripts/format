#!/bin/bash
set -e

# ─── Format ──────────────────────────────────────────────────────────────────

run_format() {
  echo "🎨 Formatting project..."
  uvx ruff format "$@"
  echo "✅ Format complete."
}

if [[ "${#BASH_SOURCE[@]}" -eq 1 ]]; then
  run_format "$@"
fi
